<p id="notice"><%= notice %></p>

Play Amusement Logo   (On every header)

<h1>Listing Events</h1>

<table id = "calendar"   >
  <thead>
    <tr>
      <th>Summary</th>
      <th>Location</th>
      <th>Description</th>
      <th>Start date</th>
      <th>Timezone</th>
      <th>End date</th>
      <th>Attendees</th>
      <th colspan="3"></th>
    </tr>
  </thead>

  <tbody>
    <% @events.each do |event| %>
      <tr>
        <td><%= event.summary %></td>
        <td><%= event.location %></td>
        <td><%= event.description %></td>
        <td><%= event.start_date %></td>
        <td><%= event.timezone %></td>
        <td><%= event.end_date %></td>
        <td><%= event.attendees %></td>
        <td><%= link_to 'Show', event %></td>
        <td><%= link_to 'Edit', edit_event_path(event) %></td>
        <td><%= link_to 'Destroy', event, method: :delete, data: { confirm: 'Are you sure?' } %></td>
      </tr>
    <% end %>
  </tbody>
</table>

<br>


<iframe id = "calendar" src="https://www.google.com/calendar/embed?src=754qcnsp17sgll46iduflhbem0%40group.calendar.google.com&ctz=America/New_York" style="border: 0" width="800" height="600" frameborder="0" scrolling="no"></iframe>





<%= link_to 'New Event', new_event_path %>
